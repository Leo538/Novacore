/**
 * Environment para producción (Vercel).
 * 
 * IMPORTANTE: Actualiza 'apiUrl' con la URL real de tu backend en Railway.
 * 
 * Esta URL se usa cuando haces build para producción.
 * Puedes cambiarla manualmente o usar variables de entorno en Vercel.
 */
export const environment = {
  production: true,
  apiUrl: 'https://tu-backend.railway.app' // ⚠️ ACTUALIZA ESTA URL con tu URL real de Railway
};

