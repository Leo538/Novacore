/**
 * Producción (Vercel). Sustituye apiUrl por la URL de tu backend desplegado
 * (ej. Railway, Render) antes de hacer build para Vercel.
 */
export const environment = {
  production: true,
  apiUrl: 'https://tu-backend.railway.app'
};
